# -*- coding: utf-8 -*-
"""项目版本号的唯一来源。

改这里，三个服务的 /docs 页标题、前端构建元数据一起生效；
git tag 与之保持一致（形如 v1.0.0）。

版本约定（SemVer）：
    1.0.0  首个正式版本：微信小店 / 小红书 / 批量发布中台三平台全流程可用。
    1.x.y  y 修 bug，x 新增功能（对外接口保持兼容）。
    2.0.0  仅当出现不兼容的接口变更时才升。
"""

APP_VERSION = "1.0.0"
